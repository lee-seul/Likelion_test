<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
  </head>
  
  <body>
    <div class="container row" style="margin: 0 auto">
      <div class="container col-md-8" style="margin-top: 50px;  left: 16%">
        <form role="form" action="/home/update/<%= @post.id%>" method="POST">
          <div class="form-group">
            <label for="email">제목</label>
            <input type="text" class="form-control" id="email" name="title" value="<%= @post.title %>">
          </div>
          <div class="form-group">
            <label for="pwd">내용</label>
            <textarea class="form-control" name="content"  rows="15"><%= @post.content %></textarea>
          </div>
          <button type="submit" class="btn btn-default">글 쓰기</button>
        </form>
      </div>
    </div>
  </body>
</html>
